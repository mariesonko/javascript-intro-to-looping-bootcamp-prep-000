function makeArray(){
  const array = []
  const strangeArray =[]
    for (let i = 0; i<25; i++){
      if (i=1) { array.push("I am 1 strange loop.")}
            return [array, i]}

    const strangeArray = []

    for (let i = 0; i < 25; i++) {
      let s = i === 1 ? "I am 1 strange loop." : `I am ${i} strange loops.`
}
